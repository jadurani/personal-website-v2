import { Ibarra_Real_Nova, Public_Sans } from "next/font/google";

export const FontHeading = Ibarra_Real_Nova({
  subsets: ["latin"],
  variable: "--font-heading"
})

export const FontBody = Public_Sans({
  subsets: ["latin"],
  variable: "--font-body"
})